const esc=v=>String(v??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const byId=id=>document.getElementById(id);
async function load(){const period=byId('period').value;const [sr,or]=await Promise.all([fetch(`/api/execution-intelligence/summary?period=${encodeURIComponent(period)}`),fetch('/api/execution-intelligence/opportunities/active')]);const s=sr.ok?await sr.json():{};const ops=or.ok?await or.json():[];byId('active').textContent=ops.length;byId('building').textContent=ops.filter(o=>o.status==='BUILDING').length;byId('weakening').textContent=ops.filter(o=>o.status==='WEAKENING').length;byId('blocked').textContent=ops.filter(o=>o.status==='BLOCKED').length;byId('ready').textContent=ops.filter(o=>o.status==='CONFIRMED').length;byId('executed').textContent=s.executed||0;byId('updated').textContent=`Updated ${new Date().toLocaleTimeString()}`;byId('queue').innerHTML=ops.length?ops.map(o=>`<article class="opportunity-card-center ${String(o.status||'watch').toLowerCase()}"><div class="opportunity-card-head"><div><strong>${esc(o.symbol)}</strong><span class="badge">${esc(o.status)}</span></div><small>${esc(o.decisionCode||'')}</small></div><div class="opportunity-progress-grid"><div class="progress-metric"><div><span>Health</span><strong>${Number(o.opportunityHealth||0)}/100</strong></div><div class="metric-bar"><i style="width:${Math.min(100,Number(o.opportunityHealth||0))}%"></i></div></div><div class="progress-metric"><div><span>Evidence</span><strong>${Number(o.evidenceScore||0)}/7</strong></div><div class="metric-bar evidence"><i style="width:${Math.min(100,Number(o.evidenceScore||0)/7*100)}%"></i></div></div></div><div class="opportunity-context opportunity-context-primary"><span>1H <b>${esc(o.oneHourDecision)}</b></span><span>5M <b>${esc(o.fiveMinuteDecision)}</b></span><span>${Number(o.buyCount||0)} BUY</span><span>${Number(o.watchCount||0)} WATCH</span></div><small class="opportunity-explanation">${esc(o.decisionExplanation||'Accumulating fresh execution evidence.')}</small></article>`).join(''):'<div class="empty">No active opportunities right now.</div>'}
byId('refresh').addEventListener('click',load);byId('period').addEventListener('change',load);load();setInterval(load,10000);
